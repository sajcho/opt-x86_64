# Description:	GStreamer streaming media framework base plug-ins
# URL:		https://gstreamer.freedesktop.org
# Maintainer:	sajcho, saux dot aarch64 at gmail dot com
# Depends on:	alsa-lib cdparanoia graphene gstreamer gtk3 libtheora libvisual libxv opus orc

name=gst-plugins-base
version=1.20.4
release=2
source=(https://gstreamer.freedesktop.org/src/${name}/${name}-${version}.tar.xz)

build() {
	cd ${name}-${version}

	# clean out
	sed 's|Wmissing-include-dirs|Wno-missing-include-dirs|' -i meson.build

	build-meson \
		-Dalsa=enabled \
		-Dintrospection=enabled \
		-Dqt5=disabled \
		-Dnls=disabled \
		-Dpackage-name="GStreamer (SAUX-x86_64)" \
		-Dpackage-origin="https://github.com/sajcho/" \
		. build

	meson compile ${JOBS:+-j ${JOBS}} -v -C build

	DESTDIR=$PKG meson install --no-rebuild -C build
}
