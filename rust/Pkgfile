# Description:	The Rust language with Cargo included.
# URL:		http://www.rust-lang.org/
# Maintainer:	sajcho, saux dot aarch64 at gmail dot com
# Depends on:	llvm-toolchain

name=rust
_realname=rustc
version=nightly-20211122
_realversion=nightly
release=1

source=(https://static.rust-lang.org/dist/${_realname}-${_realversion}-src.tar.gz \
	libexec.patch)

build() {
	cd ${_realname}-${_realversion}-src

	# Patch cargo so credential helpers are in /usr/lib instead of /usr/libexec
	patch -p1 -i $SRC/libexec.patch

	# Remove bundled dependencies.
	rm -r src/llvm-project/{clang,clang-tools-extra,libunwind,lld,lldb,llvm}
	install -d src/llvm-project/libunwind

cat <<- EOF > $SRC/config.toml
	changelog-seen = 2
	profile = "user"

	[llvm]
	ninja = true
	link-shared = true
	optimize = true
	thin-lto = true
	polly = false

	[install]
	prefix = "/usr"

	[rust]
	channel = "stable"
	codegen-units-std = 1
	debuginfo-level-std = 1
	optimize = true
	use-lld = true
	rpath = false
	codegen-tests = false
	parallel-compiler = true
	description = "SAUX x86_64"

	[build]
	target = ["$CHOST"]
	verbose = 0
	extended = true
	vendor = true
	docs = false
	profiler = false
	sanitizers = false
	python = "/usr/bin/python3"
	cargo = "/usr/bin/cargo"
	rustc = "/usr/bin/rustc"
	rustfmt = "/usr/bin/rustfmt"
	tools = ["analysis", "cargo", "clippy", "rls", "rustfmt", "src"]

	[target.$CHOST]
	llvm-config = "/usr/bin/llvm-config"
	llvm-filecheck = "/usr/bin/FileCheck"
EOF

	cat $SRC/config.toml

	mkdir "$PKGMK_SOURCE_DIR/rust" || true
	export CARGO_HOME="$PKGMK_SOURCE_DIR/rust"
	export RUST_BACKTRACE="1"
	export RUSTFLAGS="-Clinker-plugin-lto -Clinker=clang -Clink-arg=-fuse-ld=lld -Cembed-bitcode=yes"

	# prevent - if libgit2, libssh2 is installed
	export LIBGIT2_NO_PKG_CONFIG=1
	export LIBSSH2_NO_PKG_CONFIG=1

	/usr/bin/python3 ./x.py build --config="${SRC}"/config.toml ${JOBS:+-j ${JOBS}}

	DESTDIR=$PKG /usr/bin/python3 ./x.py --config="${SRC}"/config.toml install

	# Cleanup
	rm -r $PKG/usr/share/doc
	rm -r $PKG/usr/share/zsh
	rm -r $PKG/etc
	rm $PKG/usr/lib/rustlib/{components,manifest-rustc,rust-installer-version,uninstall.sh}

	rm $PKG/usr/lib/{libstd-*,libtest-*}
	ln -srft $PKG/usr/lib $PKG/usr/lib/rustlib/$CHOST/lib/*.so
}
