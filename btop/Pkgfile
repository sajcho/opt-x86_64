# Description:	A monitor of system resources, bpytop ported to C++
# URL:		https://github.com/aristocratos/btop
# Maintainer:	sajcho, saux dot aarch64 at gmail dot com
# Depends on:	gcc

name=btop
version=1.2.13
release=1
source=(https://github.com/aristocratos/btop/archive/v${version}/${name}-${version}.tar.gz)

build() {
	cd ${name}-${version}

	ADDFLAGS="-fno-ipa-cp -Wno-dangling-reference" make all

	make DESTDIR=$PKG PREFIX=/usr install

	rm $PKG/usr/share/btop/README.md

	find $PKG/usr/share -type f -name '*.desktop' \
		-exec sed -r -e '/\[.+\]=/d' -e '/#/d' -i {} \;
}
